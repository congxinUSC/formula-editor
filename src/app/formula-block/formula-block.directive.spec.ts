import { FormulaBlockDirective } from './formula-block.directive';

describe('FormulaBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FormulaBlockDirective();
    expect(directive).toBeTruthy();
  });
});
